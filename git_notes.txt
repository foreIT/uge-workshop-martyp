Git Hub:

git init = create root directory/repository for each project you work on.

git add = stage new file before committing.

git commit -m “notes for commit”

.git = hidden file/directories

git branch brandname = create new branch

git branch = list branches

git checkout branchname

git remote

git push

git clone with the clone URL in order to make a copy of the forked respository for your own possesion.
